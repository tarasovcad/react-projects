/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['avatars.githubusercontent.com', 'letsenhance.io', 'encrypted-tbn0.gstatic.com'],
  },
};

export default nextConfig;
